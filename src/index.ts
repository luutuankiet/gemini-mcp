/**
 * @luutuankiet/gemini-mcp — public API
 *
 * Exports the Gemini schema transform for programmatic use.
 * The proxy CLI is the main entrypoint (see proxy.ts / bin field).
 */

export { makeGeminiCompatible } from './lib/transforms'
